# tauri-pilot v0.5.1 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade `tauri-plugin-pilot` to `v0.5.1` and restore the textarea chat-flow pilot regression.

**Architecture:** Keep the existing debug-only, feature-gated pilot integration. Pin the upstream git dependency to the release tag and use the existing `chat-flow.toml` as the regression scenario for issue #85.

**Tech Stack:** Rust Cargo git dependencies, Tauri 2, tauri-pilot TOML scenarios, shell scripts, `just test-pilot`.

---

### Task 1: Pin pilot dependency and update lockfile

**Files:**

- Modify: `apps/agent-gui/src-tauri/Cargo.toml`
- Modify: `Cargo.lock`

- [x] Change `tauri-plugin-pilot` to `git = "https://github.com/mpiton/tauri-pilot", tag = "v0.5.1", optional = true`.
- [x] Run `cargo update -p tauri-plugin-pilot`.
- [x] Confirm `Cargo.lock` no longer references `624c9c32`.

### Task 2: Restore chat-flow textarea regression

**Files:**

- Modify: `apps/agent-gui/e2e-pilot/chat-flow.toml`

- [x] Remove the v0.5.0 textarea bug skip note.
- [x] Add steps to wait for `textarea[data-test='message-input']`, fill `Hello agent!`, click `[data-test='send-button']`, and assert `.message-user` contains `Hello agent!`.

### Task 3: Update CLI install hints

**Files:**

- Modify: `scripts/run-pilot-tests.sh`
- Modify: `justfile`
- Modify: `AGENTS.md`
- Modify: `.github/workflows/ci.yml`

- [x] Add `--tag v0.5.1` to `cargo install --git https://github.com/mpiton/tauri-pilot ... tauri-pilot-cli` examples and CI install commands.

### Task 4: Verify

- [x] Run `cargo check -p agent-gui-tauri --features pilot`.
- [x] Run the pilot scenarios with `scripts/run-pilot-tests.sh target/debug/agent-gui-tauri` after confirming `tauri-pilot-cli v0.5.1` is installed.
- [x] Run lints/diagnostics for modified files where available.
