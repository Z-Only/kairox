#[tokio::test]
async fn write_message_formats_content_length_header() {
    // Integration test requires a real subprocess — covered by client tests.
    // This is a placeholder for framing-only unit tests.
}
